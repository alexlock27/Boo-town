import { WISHES, nearest } from '../js/wishwell.js';let failed=false;const assert=(v,m)=>{console.log((v?'✓':'FAIL:'),m);if(!v)failed=true;};assert(WISHES.length===60,'the fixed Wish lexicon contains exactly 60 words');assert(nearest('moom')==='moon','nearest-word helper finds a friendly copy prompt');for(let i=0;i<200;i++){const word=Array.from({length:6},()=>String.fromCharCode(97+Math.random()*26|0)).join('');assert(typeof nearest(word)==='string','arbitrary wish input has a safe nearest response');}console.log('RESULT: '+(failed?'FAIL':'PASS'));process.exit(failed?1:0);

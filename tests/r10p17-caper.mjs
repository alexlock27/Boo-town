import { chromium } from 'playwright';
const BASE='http://127.0.0.1:8000';let failed=false;const assert=(v,m)=>{console.log((v?'✓':'FAIL:'),m);if(!v)failed=true;};const browser=await chromium.launch();const page=await browser.newPage();
await page.goto(BASE+'/index.html');await page.evaluate(()=>localStorage.setItem('bootown.save.v1',JSON.stringify({version:7,name:'Ada',guide:{},stars:{total:20,byGame:{}},town:{areas:{}},caper:{open:true,culprit:'nutmeg',clues:2,cluesToday:0,clueDay:'',guesses:0,marked:[]},seen:{trophyRetro:true},settings:{sound:false,music:false}})));await page.reload();await page.waitForSelector('.hub');assert(await page.locator('.trail-chip.caper').count()===1,'open caper appears in Today');await page.evaluate(()=>window.BooTown.go('caper'));await page.waitForSelector('.caper-suspect');await page.locator('.caper-suspect').first().click();assert(await page.locator('.caper-suspect.down').count()===1,'suspect marks are reversible');await page.locator('.caper-suspect').first().click();assert(await page.locator('.caper-suspect.down').count()===0,'second tap reverses notebook mark');await browser.close();console.log('RESULT: '+(failed?'FAIL':'PASS'));process.exit(failed?1:0);
